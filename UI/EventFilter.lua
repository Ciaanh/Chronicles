local FOLDER_NAME, private = ...
local Chronicles = private.Core

Chronicles.UI.EventFilter = {}

function Chronicles.UI.EventFilter:DisplayEventFilter(page)
    
end

function EventFilter_OnClick(self)
    
end
